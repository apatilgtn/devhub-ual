# DevHub UAL – environment variables
# Copy to .env and fill in values. For Railway, set these in the project Variables.

# -----------------------------------------------------------------------------
# Required for production (Railway)
# -----------------------------------------------------------------------------
POSTGRES_HOST=
POSTGRES_PORT=5432
POSTGRES_USER=
POSTGRES_PASSWORD=

# Public URL of the deployed app (e.g. https://your-service.up.railway.app).
# Railway: set after first deploy or use "Generate domain" in dashboard/MCP.
APP_BASE_URL=

# -----------------------------------------------------------------------------
# Optional
# -----------------------------------------------------------------------------
# Supabase Auth (CustomSignInPage / LogoutPage). Set in Railway for production.
SUPABASE_URL=
SUPABASE_ANON_KEY=

# GitHub token for catalog/integrations (see app-config.yaml integrations.github).
GITHUB_TOKEN=

# GitHub OAuth (auth.providers.github.development). Required if using GitHub login.
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# Backend service-to-service auth (uncomment auth.keys in app-config if used).
# BACKEND_SECRET=

# Local dev only (production uses POSTGRES_*).
# POSTGRES_HOST=localhost
# POSTGRES_PORT=5432
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=
